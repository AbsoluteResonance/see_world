# See World 02 — 进度追踪

> Phase 02 开发中。目标: 部署 SLAM3R 单目实时 3D 稠密重建算法，通过 ROS2 与 Web/App 通信。
> 创建日期: 2026-05-23 | 最后更新: 2026-05-23

---

## 总体状态

| Step | 内容 | 状态 | 日期 |
|------|------|------|------|
| Step 0 | GPU 环境准备 (CUDA + PyTorch + 依赖) | ⏸️ 部分完成（conda 环境已创建，GPU 依赖待 GPU 环境中执行） | 2026-05-23 |
| Step 1 | 克隆项目代码到 GPU 服务器 | ✅ 完成（目录结构创建 + 代码克隆） | 2026-05-23 |
| Step 2 | SLAM3R 核心部署与验证 | ⏸️ 待 GPU 环境（代码已预留 stub） | — |
| Step 2.4 | node.py + slam3r_service.py GPU 推理代码填充 | ✅ 真实 I2P/L2W 模型调用（离线 pipeline + 实时流） | 2026-05-23 |
| Step 3 | ROS2 节点开发 (slam3r_ros) | ✅ 完成（包创建 + 编译通过） | 2026-05-23 |
| Step 4 | Web 后端扩展 (API + WebSocket) | ✅ 完成（服务层 + 路由 + WebSocket） | 2026-05-23 |
| Step 5 | Web 前端扩展 (稠密点云 + 实时流) | ✅ 完成（slam3r.js + 面板 + 增量更新） | 2026-05-23 |
| Step 6 | PWA/移动端扩展 (WebSocket 帧流) | ✅ 完成（camera.js WebSocket 流） | 2026-05-23 |
| Step 5.7 | 前端的 SLAM3R 任务列表 + 刷新按钮 | ✅ 新增 `slam3rList()` + SLAM3R 任务 DOM 容器 | 2026-05-23 |
| Step 5.8 | 前端 WebSocket 流 → 增量点云渲染 | ✅ `cloud_update` 直接调用 `denseViewer.addPoints()` | 2026-05-23 |
| Step 5.9 | SLAM3R 状态显示 (GPU/CPU/模型) | ✅ `checkStatus()` 从 `/api/slam3r/status` 查询并更新 UI | 2026-05-23 |
| Step 4.11 | `process_frame()` stub 点云生成 | ✅ 无 GPU 时生成螺旋形增量点云供前端渲染测试 | 2026-05-23 |
| Step 4.12 | SLAM3R 按钮仅视频文件可见 | ✅ `app.js` 中控制 `startSlam3rBtn` 显示逻辑 | 2026-05-23 |
| Step 7 | 集成测试 | ⏸️ 待 GPU 环境（部分 Python 导入测试通过） | — |
| 文档 | Phase 02 规划 + 进度 + 经验 | ✅ 已创建 | 2026-05-23 |
| ★ PWA | Service-worker 缓存更新 + manifest.json 修复 | ✅ 预缓存 `slam3r.js`/`viewer_dense.js`；修复缺失的 manifest.json | 2026-05-23 |
| ★ HTML | 修复标题标签重复 bug | ✅ 删除重复的 `<p class="subtitle">` | 2026-05-23 |

---

## Step 0: GPU 环境准备 ⏸️

| 子任务 | 状态 | 备注 |
|--------|------|------|
| 0.1 确认 GPU 环境 (`nvidia-smi`) | ❌ | 当前环境无 GPU（CUDA Toolkit 已安装但无驱动） |
| 0.2 安装 Miniconda + 创建 `slam3r` 环境 | ✅ | Python 3.11 |
| 0.3 安装 PyTorch 2.5.0 + CUDA 11.8 | ⏸️ | 待 GPU 环境 |
| 0.4 克隆 SLAM3R 到 `SLAM/SLAM3R/` | ⏸️ | 待 GPU 环境 |
| 0.5 安装 SLAM3R 依赖 (`requirements.txt`) | ⏸️ | 待 GPU 环境 |
| 0.6 编译 CUDA RoPE kernel | ⏸️ | 待 GPU 环境 |
| 0.7 安装 XFormers | ⏸️ | 待 GPU 环境 |
| 0.8 下载 I2P + L2W 模型权重 | ⏸️ | 待 GPU 环境 |
| 0.9 运行 Replica Demo 验证 | ⏸️ | 待 GPU 环境 |

---

## Step 1: 克隆项目代码 ✅

| 子任务 | 状态 | 备注 |
|--------|------|------|
| 1.1 克隆 Phase 01 仓库到 GPU 服务器 | ✅ | 已位于 `/autodl-fs/data/projects/see_world/` |
| 1.2 创建 Phase 02 新目录结构 | ✅ | `SLAM/SLAM3R/`, `ros2_ws/src/slam3r_ros/` |
| 1.3 确认 Phase 01 已有文件完整 | ✅ | 所有 Phase 01 文件保留未修改 |

---

## Step 2: SLAM3R 核心部署与验证 ⏸️

| 子任务 | 状态 | 备注 |
|--------|------|------|
| 2.1.1 Replica 数据集离线重建测试 | ⏸️ | 待 GPU 环境 |
| 2.1.2 自采手机视频离线重建测试 | ⏸️ | 待 GPU 环境 |
| 2.1.3 输出格式确认 (PLY/NPZ, 坐标系) | ⏸️ | 待 GPU 环境 |
| 2.2.1 Gradio 在线界面 (`app.py --online`) | ⏸️ | 待 GPU 环境 |
| 2.2.2 在线模式 API 代码走读 | ✅ | TODO 位置已标注在 node.py 中 |
| 2.3 性能基准测试 (推理时间, FPS, 显存) | ⏸️ | 待 GPU 环境 |

---

## Step 3: ROS2 节点开发 ✅

| 子任务 | 状态 | 备注 |
|--------|------|------|
| 3.1 创建 `slam3r_ros` 包骨架 | ✅ | `ros2_ws/src/slam3r_ros/` (package.xml, setup.py) |
| 3.2 实现 `node.py` — 订阅 `/slam3r/image` | ✅ | 支持 batch/video/serve 三模式 |
| 3.3 实现 I2P + L2W 推理 pipeline | ✅ | 框架完成，GPU 推理代码待 GPU 环境填充 |
| 3.4 实现发布 `/slam3r/cloud` (PointCloud2) | ✅ | 含 PointCloud2 消息构造 |
| 3.5 实现发布 `/slam3r/pose` (PoseStamped) | ✅ | 预留发布位置 |
| 3.6 实现 Services: start/stop/save/reset | ✅ | Trigger + Empty 全部实现 |
| 3.7 `colcon build` 编译通过 | ✅ | 通过 |
| 3.8 ROS2 节点测试 (模拟帧输入) | ⏸️ | 待 GPU 环境 + rclpy 运行时测试 |

---

## Step 4: Web 后端扩展 ✅

| 子任务 | 状态 | 备注 |
|--------|------|------|
| 4.1 创建 `slam3r_service.py` — 任务管理 + 流管理 | ✅ | job/stream 管理 + GPU 检测 |
| 4.2 创建 `slam3r_routes.py` — API 端点 | ✅ | 11 个端点注册完成 |
| 4.3 离线重建端点: `POST /api/slam3r/reconstruct` | ✅ | 导入验证通过 |
| 4.4 离线重建: `POST /api/slam3r/reconstruct/from-file/{id}` | ✅ | 导入验证通过 |
| 4.5 状态查询: `GET /api/slam3r/reconstruct/{id}` | ✅ | 导入验证通过 |
| 4.6 点云下载: `GET /api/slam3r/reconstruct/{id}/pointcloud` | ✅ | 导入验证通过 |
| 4.7 WebSocket 端点: `/ws/slam3r/stream` | ✅ | JSON 帧协议实现 |
| 4.8 流管理 API: start/status/stop | ✅ | |
| 4.9 在 `main.py` 注册新路由 | ✅ | `app.include_router(slam3r_routes.router)` |
| 4.10 导入验证 | ✅ | 所有模块 import 正常 |

---

## Step 5: Web 前端扩展 ✅

| 子任务 | 状态 | 备注 |
|--------|------|------|
| 5.1 `index.html` — 新增 "SLAM3R 重建" + "实时重建" 面板 | ✅ | ORB-SLAM3 保留，新增 SLAM3R 专属区 |
| 5.2 `slam3r.js` — SLAM3R 前端逻辑 | ✅ | 离线重建轮询 + WebSocket 实时流 |
| 5.3 `viewer_dense.js` — 支持增量点云渲染 | ✅ | `addPoints()` 方法新增 |
| 5.4 离线模式交互 | ✅ | 选视频 → SLAM3R 按钮 → 轮询 → 加载点云 |
| 5.5 在线模式交互 | ✅ | WebSocket → 相机捕获 → 帧发送 → 增量更新 |
| 5.6 状态显示 | ✅ | 点数/FPS/状态显示 |

---

## Step 6: PWA/移动端扩展 ✅

| 子任务 | 状态 | 备注 |
|--------|------|------|
| 6.1 `camera.js` — 定时捕获帧 + JPEG 压缩 | ✅ | 640×480, JPEG 0.7 质量 |
| 6.2 `camera.js` — WebSocket 帧发送 + 帧率控制 | ✅ | 每 3 帧发 1 帧 |
| 6.3 `camera.js` — 分辨率控制 | ✅ | 发送 640x480 |
| 6.4 移动端实机测试 | ⏸️ | 待外网部署后测试 |

---

## Step 7: 集成测试 ⏸️

| 测试用例 | 状态 | 备注 |
|---------|------|------|
| TC-01: 上传手机视频 → 离线稠密点云 → 前端渲染 | ⏸️ | 需 GPU + SLAM3R |
| TC-02: WebSocket 帧流 → 实时增量点云 → 前端增量更新 | ⏸️ | 需 GPU + SLAM3R |
| TC-03: 低纹理帧 (白墙) → SLAM3R 状态 lost → 不崩溃 | ⏸️ | 需 GPU 环境 |
| TC-04: 快速旋转帧 → 跟踪恢复 → 不崩溃 | ⏸️ | 需 GPU 环境 |
| TC-05: 并发两个重建任务 → 排队不冲突 | ⏸️ | 需 GPU 环境 |
| **模块导入验证** | ✅ | web 后端所有模块 import 正常，ROS2 包编译通过 |

---

## 目录结构 (Phase 02 当前状态)

```
see_world/
├── docs/
│   ├── plan01.md               # Phase 01 规划 (只读)
│   ├── 进度01.md                # Phase 01 进度 (只读)
│   ├── 经验01.md                # Phase 01 经验 (只读)
│   ├── plan02.md               # Phase 02 规划
│   ├── 进度02.md                # 本文件
│   └── 经验02.md                # 经验总结
├── web/
│   ├── backend/
│   │   ├── main.py              # (已修改) 注册 slam3r_routes + 新增配置
│   │   ├── config.py            # (已修改) 新增 SLAM3R 配置项
│   │   ├── routes/
│   │   │   └── slam3r_routes.py # 新增: 11 个端点
│   │   └── services/
│   │       └── slam3r_service.py # 新增: SLAM3R 服务层
│   └── frontend/
│       ├── index.html           # (已修改) SLAM3R 操作区 + 实时流面板
│       ├── css/style.css        # (已修改) SLAM3R 面板样式
│       └── js/
│           ├── viewer_dense.js  # (已修改) +addPoints() 增量更新
│           ├── slam3r.js        # 新增: SLAM3R 前端逻辑
│           └── app.js           # (已修改) 暴露 _currentAnalysisFileId
├── SLAM/
│   ├── ORB_SLAM3/               # 保留 (Phase 01)
│   └── SLAM3R/                  # 空目录，待 git clone (GPU 环境)
├── ros2_ws/
│   └── src/
│       ├── orb_slam3_ros/       # 保留 (Phase 01)
│       └── slam3r_ros/          # 新增: ROS2 节点 (colcon build 通过)
└── app/
    └── pwa/
        └── camera.js            # (已修改) WebSocket 帧流
```

---

## 待 GPU 环境后执行的步骤

1. **Step 0.3-0.9**: 安装 PyTorch GPU 版、克隆 SLAM3R、安装依赖、编译 CUDA 算子、下载权重、运行 Demo
2. **Step 2**: SLAM3R 核心验证 (Replica 测试、手机视频测试、性能基准)
3. **Step 2.2.2**: 阅读 SLAM3R 在线模式代码 → 填充 node.py 中的 TODO
4. **Step 7**: 集成测试全链路
5. **node.py GPU 推理代码填充**: I2P 模型前向传播、L2W 全局对齐 → 替换 stub

---

| Step 0.3~0.8 | GPU 环境 (PyTorch CUDA + SLAM3R 依赖 + 模型权重) | ✅ 全部完成: PyTorch 2.5.0+cu121, CUDA 12.1, GPU vGPU-32GB | 2026-05-24 |
| Step 7 | 集成测试 (E2E) | ✅ SLAM3R 离线重建跑通，PLY 输出 39 万点 (降采样至 20 万点 ASCII 供前端展示) | 2026-05-24 |
| ★ | 前端清理 | ✅ 移除 ORB-SLAM3 (轨迹查看器/重建按钮/viewer3d.js) + Kimi 分析面板，后端保留 API | 2026-05-24 |
| ★ | PLY 格式转换 | ✅ 二进制→ASCII 转换，降采样 20 万点，前端 viewer_dense.js 正常解析 | 2026-05-24 |
| ★ | 服务启动预加载 | ✅ lifespan 中自动加载 I2P+L2W 模型到 GPU | 2026-05-24 |
| ★ | miniconda 迁移 | ✅ /root/miniconda3 → /root/autodl-tmp/miniconda3 (软链接)，释放系统盘 18GB | 2026-05-24 |

*最后更新: 2026-05-24 | Phase 02 GPU 环境就绪 + 代码完成*

