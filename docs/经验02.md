# See World — 开发经验总结 (Phase 02)

> 记录 Phase 02 开发过程中遇到的关键问题、解决方案和技术决策。
> 创建日期: 2026-05-23 | 最后更新: 2026-05-25

---

## 一、架构演进

### 原始计划: SLAM3R + ROS2 + Web
- SLAM3R (I2P/L2W) 稠密重建 → ROS2 节点 → Web 前端
- 依赖 HuggingFace 模型下载，需要 GPU

### 实际方案: MASt3R-SLAM + HTTP API + Web/Android
- MASt3R-SLAM (实时稠密 SLAM) → HTTP POST 帧 → 点云轮询
- 无 ROS2，纯 Python 子进程通信
- 前端: HTTP fetch 替代 WebSocket（cloudflare 隧道对 WebSocket 不稳定）

### 移除的组件
- SLAM3R (模型太大，HuggingFace 下载不稳定)
- ROS2 (Python 3.10 限制，增加复杂度无实际收益)
- VINS-Mono (IMU+VO，效果差)

---

## 二、关键技术问题

### 2.1 WebSocket vs HTTP POST

| 问题 | 方案 |
|------|------|
| cloudflared 隧道 WebSocket 连接不稳定，经常断开 | 改用 HTTP POST 每帧独立请求，fire-and-forget |
| 点云响应太大 (~2MB)，隧道传不回来 | 拆分 `/frame`（轻量确认）和 `/points`（单独拉取）两张端点 |
| gzip 压缩 | 5000 点 JSON ~170KB → 压缩后 ~30KB |

### 2.2 3D 查看器问题

| 问题 | 解决方案 |
|------|----------|
| `denseViewer.init()` 抛异常导致帧循环不启动 | 用 try-catch 包裹 init() 调用 |
| `statusEl` 闭包引用失败 | 改为函数内部 `document.getElementById()` 重新获取 |
| `canvas.toBlob` + `FileReader` 在 X 浏览器上不稳定 | 改用 `canvas.toDataURL()` 同步转换 |
| Service Worker 缓存旧 JS | 添加 `Cache-Control: no-store` 头 + 强制 unregister |
| Y 轴/ Z 轴方向错误 | 在推理子进程中取反 Y 和 Z 坐标 |

### 2.3 MASt3R-SLAM 推理问题

| 问题 | 解决方案 |
|------|----------|
| tracker 不产生新关键帧，点云不更新 | 强制每 3 帧添加新关键帧 |
| `Boolean value of Tensor with more than one value is ambiguous` | `match_frac = valid_opt.sum().item() / valid_opt.numel()` |
| 同上错误在 `len(kf.X_canon or [])` | 去掉 `or []`，直接用 `len(kf.X_canon)`；PyTorch 张量不支持 `or` 操作 |
| 点云只从最新关键帧提取 | 改为从所有关键帧提取，每帧取 `pts_per_kf` 个点 |

### 2.4 帧传输与保存

| 问题 | 解决方案 |
|------|----------|
| 帧传输不稳定，不知帧是否到达服务器 | 创建独立帧测试页 `/static/frame_test.html` |
| 需要查看实际收到的帧 | 添加"保存帧"复选框，帧存到 `/tmp/mast3r_frame_*.jpg` |

### 2.5 服务端问题

| 问题 | 解决方案 |
|------|----------|
| `time.sleep(0.2)` 阻塞事件循环，后续请求排不上 | 改为 `await asyncio.sleep(0.2)` |
| 模型加载慢（~30s） | 启动时预加载推理子进程 |

---

## 三、通信链路变更

### 从 cloudflared 隧道切换到 AutoDL 端口映射

| 之前 | 之后 |
|------|------|
| cloudflared tunnel → trycloudflare.com 域名 | AutoDL 自定义服务映射端口 6006 |
| 延迟 ~5s（Cloudflare 全球网络绕行） | 延迟低（内网反代） |
| 隧道重启域名变更，APP 需重新配置 | 域名固定 |
| WebSocket 不稳定 | HTTP 稳定 |

**服务器地址**: `https://u59647-a141-5d60e0cc.westc.seetacloud.com:8443`

AutoDL 只开放 6006/6008 两个端口映射（个人用户不可新增）。将 uvicorn 改绑到端口 6006 即可利用现成的公网映射。

---

## 四、离线批量重建

### 功能
上传视频后自动触发离线稠密重建，无需实时相机流：
1. 上传视频 → 自动提取帧 → MASt3R 逐帧推理 → 累积点云 → 保存 PLY
2. 自动生成 4 视角截图（前/后/左/右）
3. 用户可下载 PLY 点云文件或截图 zip

### 实现
- `ros2_ws/src/slam3r_ros/slam3r_ros/mast3r_batch_standalone.py`
  - 独立子进程脚本，Python 3.11 (slam3r env)
  - 协议: stdin/stdout JSON（与 streaming 子进程一致）
  - OpenCV 提取帧，letterbox 缩放 640x480
  - 复用 `mast3r_infer_standalone.py` 的推理流程（create_frame → mast3r_inference_mono → FrameTracker.track）
  - 保存 PLY 使用 `plyfile` 库
  - 截图使用 matplotlib Agg 后端（headless 安全）

### 踩过的坑

| 问题 | 解决 |
|------|------|
| Open3D `get_view_control()` 在 headless 下返回 None | 改用 matplotlib Agg 渲染截图 |
| `len(kf.X_canon or [])` 引发 RuntimeError | `kf.X_canon` 是 PyTorch 张量，`or` 触发 bool() 报错 |

---

## 五、每帧点数可配置

实时重建时，用户可通过输入框 + "确认"按钮控制每帧返回的点数：
- 前端 `slam3r.js` 读取 `maxPointsInput`，存入 `mast3rMaxPoints` 变量
- 每帧 POST 时携带 `max_points` 字段
- 子进程用 `int(max_points) // num_keyframes` 计算 `pts_per_kf`
- 范围: 50-20000，默认 500

---

## 六、视频上传改造

- 移除图片上传，仅接受 `video/*`
- 删除已有图片文件
- 新增 "上传后自动离线重建" 复选框
- 画廊条目增加 "重建点云" 按钮（hover 可见）
- 后端 `upload.py` 精简，只保留 `VIDEO_DIR`

---

## 七、Android APP 构建

### 编译流程
- 项目: 最小 WebView APP，加载 MASt3R-SLAM 前端
- CI: GitHub Actions + `android-actions/setup-android@v3`
- Gradle 8.5 + compileSdk 34 + AndroidX

### 踩过的坑

| 问题 | 解决 |
|------|------|
| `gradle-wrapper.jar` 被 .gitignore 排除 | 从 .gitignore 移除并 `git add -f` 强制提交 |
| `gradlew` 的 `$APP_HOME` 未设置 | 添加路径解析逻辑 |
| Kotlin DSL: `include ':app'` 错误 | 改为 `include(":app")` |
| AndroidX 依赖未启用 | 创建 `gradle.properties` 添加 `android.useAndroidX=true` |
| Release 创建 403 | 添加 `permissions: contents: write` |
| APK 文件名不匹配 | 用 `cp` 重命名为 `SeeWorld-v1.0.0.apk` |
| 默认 URL 硬编码为 cloudflared 域名 | 更新为 AutoDL 公网地址，后续版本更新 |

---

## 八、最终架构

```
┌─ 实时重建 ──────────────────────────────┐
│ 手机相机 → canvas → HTTP POST /frame    │
│  → MASt3R-SLAM 子进程 → 点云 JSON       │
│  → 前端轮询 /points → Three.js 渲染     │
├─ 离线批量重建 ───────────────────────────┤
│ 上传视频 → 自动提取帧 → MASt3R 批处理   │
│  → 保存 PLY + 截图 → 下载/查看          │
└──────────────────────────────────────────┘

启动: python start_mast3r.py  (端口 6006)
访问: https://u59647-a141-5d60e0cc.westc.seetacloud.com:8443
GitHub: https://github.com/AbsoluteResonance/see_world
```

### 性能
- 每帧推理: ~500ms (GPU)
- 每帧点数: 可配置 (50-20000, 默认 500)
- 帧间隔: 1.5s (前端) / 3s (点云轮询)
- 离线重建: 6MB 视频 15 帧 → ~67 万点，10s 完成
- 模型加载: ~30s
